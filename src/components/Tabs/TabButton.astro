---
const { id, label } = Astro.props;
---

<button
  class="tab-btn cursor-pointer px-4 py-2 text-foreground text-[13px]"
  id={`tab-btn-${id}`}
  onclick={`switchTab('${id}')`}
>
  {label}
</button>
